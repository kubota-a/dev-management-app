## ■ ログイン認証


### ログイン処理
【実装確認】
- [x] Flask-WTF が導入されている
- [x] CSRFProtect が有効化されている
- [x] form.hidden_tag() によるCSRFトークンが使用されている
- [x] check_password_hash によるパスワード検証が行われている
- [x] login_user が使用されている
- [x] is_active=False のユーザーをログイン不可にしている
- [x] 認証失敗時のメッセージが統一されている
- [x] インラインエラーが最初の1件のみ表示される

---

【手動テスト】
- [x] 正しいID・パスワードでログインできる
- [x] 間違ったパスワードでログインできない
- [x] ID未入力でエラー表示される
- [x] パスワード未入力でエラー表示される
- [x] ログイン失敗後、IDは保持される
- [x] ログイン失敗後、パスワードは空になる

---

### ロール別遷移
【実装確認】
- [x] ロール別リダイレクト関数が存在する
- [x] applicant / manager / hq で遷移先が分岐されている

---

【手動テスト】
- [x] applicant でログインすると申請者トップへ遷移する
- [x] manager でログインすると部門管理者トップへ遷移する
- [x] hq でログインすると本部管理者トップへ遷移する

---

### ログアウト処理
【実装確認】
- [x] /logout が POST で実装されている
- [x] logout_user が使用されている
- [x] login_required が付与されている

---

【手動テスト】
- [x] ログアウトボタンでログアウトできる
- [x] ログアウト後にログイン画面へ戻る

---

### トースト表示
【実装確認】
- [x] _toast.html が存在する
- [x] flash メッセージをトーストとして表示している
- [x] success メッセージのみ表示対象としている

---

【手動テスト】
- [x] ログイン成功時にトーストが表示される
- [x] ログアウト成功時にトーストが表示される
- [x] トーストが数秒後に自動で消える

---

### ログイン制御
【実装確認】
- [x] login_required がトップ画面に付与されている
- [x] login_manager.login_view が設定されている

---

【手動テスト】
- [x] 未ログインでトップURLにアクセスするとログイン画面へ戻る
- [x] ログイン済みで /login にアクセスするとトップへリダイレクトされる

---

### 軽微な品質改善
【実装確認】
- [x] ロール別リダイレクト関数が role を受け取る形に整理されている
- [x] login.html の required 指定が自然な形に修正されている
- [x] success の flash が汎用的にトースト表示される
- [x] 未使用 import が残っていない

---

【手動テスト】
- [x] 正しいID・パスワードでログインできる
- [x] ログイン成功時にトーストが表示される
- [x] ログアウトできる
- [x] ログアウト成功時にトーストが表示される

---

## ■ 申請者：新規案件申請画面
実施：

### 新規案件申請処理
【実装確認】
- [x] 申請者のみアクセスできる権限チェックがある
- [x] 部門一覧をDBから取得してプルダウン表示している
- [x] 申請者名をログイン中ユーザーから表示している
- [x] 申請時にProjectを作成している
- [x] 申請時にstatus / approval_stageを department_pending にしている
- [x] 申請時にProjectStatusLogを作成している
- [x] 部門管理者向けNotificationを作成している
- [x] 申請成功後、申請内容確認画面へリダイレクトしている
- [x] 申請者本人へのNotificationは作成していない
- [x] 開始予定日・完了予定日はDate型として保存している
- [x] 案件番号はREQ-YYYY-xxxxx形式で生成している
- [x] 案件番号の年はAsia/Tokyo基準で生成している

---

【手動テスト】
- [x] 申請者で新規案件申請画面を表示できる
- [x] 案件名・部門・目的・予算・工数を入力して申請できる
- [x] 申請成功後、申請内容確認画面へ移動する
- [x] 申請内容確認画面に案件番号・案件名・現在ステータスが表示される
- [x] 現在ステータスが「部門承認待ち」と日本語で表示される
- [x] 必須項目が未入力の場合、申請確認モーダルへ進まずエラー表示される

---

### 下書き保存・下書き反映
【実装確認】
- [x] 自分の下書きのみ取得して表示している
- [x] 下書き保存APIが実装されている
- [x] 下書き削除APIが実装されている
- [x] 下書きは最大5件に制限している
- [x] 6件目保存時は古い下書きを削除する処理がある
- [x] 下書き保存時に開始予定日・完了予定日も保存している
- [x] 下書き反映時に開始予定日・完了予定日もフォームへ反映している
- [x] 下書き保存後、フォームを初期化している
- [x] 下書き保存後、開始予定日・完了予定日もクリアしている
- [x] 下書き保存時のtoastは「下書きを保存しました」のみ表示する
- [x] 下書き反映時、入力済み内容がある場合は上書き確認を表示する
- [x] 上書き確認の判定に工数・開始予定日・完了予定日も含めている

---

【手動テスト】
- [x] 入力途中の内容を下書き保存できる
- [x] 下書き保存後、フォームが空になる
- [x] 「下書きから作成」ボタンで下書きドロワーが開く
- [x] 保存した下書きをフォームに反映できる
- [x] 下書き反映後、「反映内容をクリア」でフォームが空になる
- [x] 入力済みの状態で下書きを反映しようとすると、上書き確認が表示される
- [x] 下書きを削除できる

---

### 備考
- 案件番号の同時申請時の再採番リトライは、今回は実装対象外とする
- 再採番リトライは課題2のプレゼンテーションで今後の改善として扱う

---

## ■ 申請者：申請状況確認・管理画面

### 申請状況確認画面の表示・権限制御
【実装確認】
- [x] `/applicant/projects/<int:project_id>/status` のGETルートが実装されている
- [x] `@login_required` によりログイン必須になっている
- [x] applicant以外はアクセスできない権限チェックがある
- [x] 自分の案件のみ表示する所有者チェックがある
- [x] 表示対象ステータスが `department_pending` / `hq_pending` / `rejected` に限定されている
- [x] 開発中・完了済み案件はこの画面の対象外になっている
- [x] 存在しない案件・他人の案件は申請者トップへ戻す処理がある
- [x] 新規案件申請後の遷移先が申請状況確認画面になっている

---

【手動テスト】
- [x] 申請者で申請状況確認画面を表示できる
- [ ] 申請者以外でアクセスすると権限エラーになり、ロール別トップへ戻る
- [x] 開発中・完了済み案件のURLにアクセスすると申請者トップへ戻る

---

### 案件切り替え・申請内容表示
【実装確認】
- [x] 案件切り替えバーのUIを維持している
- [x] プルダウンに本人の承認待ち・却下案件のみ表示している
- [x] プルダウン選択時に対象案件の `/status` URLへ遷移する
- [x] 案件名・案件番号・申請者名・担当部門・目的概要をDB値で表示している
- [x] 概算予算と工数を表示用に整形している
- [x] 予定実施期間を表示している
- [x] 予定実施期間は片方だけ入力されている場合にも対応している
- [x] ダミーデータではなくDB値を表示している

---

【手動テスト】
- [x] 案件切り替えプルダウンで別案件へ切り替えられる
- [x] 案件名・案件番号・申請者名・担当部門・目的概要が表示される
- [x] 概算予算・工数・予定実施期間が表示される

---

### ステータス・ステッパー・日時表示
【実装確認】
- [x] 現在ステータスを日本語ラベルで表示している
- [x] ステータスに応じてステッパー表示を切り替えている
- [x] `ProjectStatusLog` がない場合も画面が壊れないフォールバックがある
- [x] 申請日は `submit` ログを優先し、なければ `project.created_at` を使う
- [x] 申請日表示は時刻なしの日本語日付になっている
- [x] DateTime型はJSTへ変換して表示している
- [x] Date型はUTC変換せず業務日付として表示している
- [x] Windowsでエラーになる `%-m` / `%-d` を使っていない

---

【手動テスト】
- [x] 現在ステータスが日本語で表示される
- [x] 申請日が「2026年5月4日」のように時刻なしで表示される
- [x] ステッパーがステータスに応じて表示される

---

### 却下案件の表示・未実装導線
【実装確認】
- [x] 却下案件のみ却下理由パネルを表示している
- [x] 却下理由が空の場合の表示文言がある
- [x] 却下案件のみ固定フッターに「修正して再申請」「削除」ボタンを表示している
- [x] 承認待ち案件では固定フッターの操作ボタンを非表示にしている
- [x] 「修正して再申請」はDB処理せずtoast案内のみになっている
- [x] 「削除」は確認モーダル表示までで、DB削除は行わない
- [x] 削除POSTルートを作成していない
- [x] 再申請ルートを作成していない
- [x] JavaScript内の案件名埋め込みに `tojson` を使っている

---

【手動テスト】
- [x] 却下案件で却下理由パネルが表示される
- [x] 却下案件で「修正して再申請」「削除」ボタンが表示される
- [x] 承認待ち案件では「修正して再申請」「削除」ボタンが表示されない
- [x] 「修正して再申請」を押すと未実装案内のtoastが表示される
- [x] 「削除」を押すと確認モーダルが表示される
- [x] 確認モーダル内の「削除」を押すと未実装案内のtoastが表示される

---

## ■ 部門管理者：承認審査画面

### 承認審査画面の表示・権限・案件切り替え
【実装確認】
- [x] `/manager/projects/review` から自部門の承認待ち案件へ遷移する
- [x] `/manager/projects/<project_id>/review` で承認審査画面を表示する
- [x] 部門管理者ロール以外は表示できない
- [x] 自部門以外の案件は審査できない
- [x] `department_pending` / `approval_stage = department_pending` の案件だけ審査対象にしている
- [x] 承認待ち案件がない場合は空状態画面を表示する
- [x] 案件切り替えは自部門の承認待ち案件だけを対象にしている
- [x] 案件名・申請者・部門・目的概要・概算予算・工数・予定期間・申請日・案件番号が実データで表示される

---

【手動テスト】
- [x] 部門管理者アカウントで `/manager/projects/review` にアクセスすると承認審査画面が開く
- [x] 案件名・申請者・目的概要・概算予算・工数・予定期間・申請日・案件番号が画面に表示される
- [x] 案件切り替え矢印または審査待ち案件リストから、別の承認待ち案件へ移動できる
- [x] 申請者アカウントで承認審査画面URLにアクセスすると、権限エラーになり自分のトップ画面へ戻る

---

### 部門年間予算シミュレーション
【実装確認】
- [x] 予算年度は `planned_start_date` が属する日本年度で判定している
- [x] 部門年間予算は `DepartmentYearlyBudget.annual_budget_amount` を使用している
- [x] 実績額は同一部門・同一年度・却下以外の案件に紐づく `BudgetActualLog.amount` を集計している
- [x] この案件の金額は承認前の概算予算を使用している
- [x] 予算消化率・承認後残予算・本案件の部門占有率を計算している
- [x] 部門内予算順位は同一部門・同一年度・却下以外の案件を対象にしている
- [x] 予算順位では `approved_budget_amount` があれば優先し、なければ `estimated_budget_amount` を使っている
- [x] 予算消化率・予算残高・本案件の部門占有率の重要数値テキスト色が動的に切り替わる
- [x] 横軸目盛りは固定値ではなく、年間予算に応じた `0 / 25% / 50% / 75% / 100%` 表示になっている
- [x] 割合表示で `10%` が `1%` にならないように整形している
- [x] 予算超過時の残予算表示は `-¥1,000,000` 形式になる

---

【手動テスト】
- [x] 予算カードに年間予算・実績額・この案件・承認後残予算が表示される
- [x] 横軸目盛りが `0 / 25% / 50% / 75% / 100%` と金額付きで表示される
- [x] 本案件の部門占有率が、凡例の「この案件」の割合と矛盾していない
- [x] 予算カードの重要数値テキスト色が不自然な固定表示になっていない
- [x] 棒グラフと凡例が画面上で大きく崩れていない

---

### 承認・却下アクション
【実装確認】
- [x] 初期表示は「承認」選択になる
- [x] 承認時はコメント欄を入力不可にしている
- [x] 却下時は却下理由欄を表示し、入力可能にしている
- [x] 却下理由テンプレートを押すとコメント欄へ反映される
- [x] フッターの却下ボタン押下時、却下理由が空なら自動で「却下」選択へ切り替わる
- [x] 却下理由が空の場合は、却下理由欄に赤い縁取りとトーストを表示する
- [x] 承認・却下は確認モーダル経由で実行する
- [x] POST時に `action` と `confirmed` をサーバー側でも検証している
- [x] 却下理由はサーバー側でも必須チェックしている
- [x] 却下理由は500文字以内でサーバー側チェックしている
- [x] 承認時は `hq_pending` に更新し、ステータスログと通知を作成する
- [x] 却下時は `rejected` に更新し、却下理由・ステータスログ・通知を作成する
- [x] 処理後は次の承認待ち案件へ遷移する

---

【手動テスト】
- [x] 初期表示で「承認」が選択されている
- [x] 「却下」を選ぶと却下理由欄と却下理由テンプレートが表示される
- [x] 却下理由テンプレートを押すとコメント欄に文言が入る
- [x] 却下理由が空のまま「却下」ボタンを押すと、トーストと赤い縁取りが表示される
- [x] 「承認」ボタンを押すと確認モーダルが表示され、キャンセルで戻れる
- [x] 「却下」ボタンを押すと確認モーダルが表示され、キャンセルで戻れる
- [x] 実際に1件だけ承認または却下し、処理後に次の承認待ち案件へ移動する

---

## ■ 本部管理者：最終承認審査画面

### 画面表示・権限制御
【実装確認】
- [x] `/hq/projects/final-review` ルートが実装されている
- [x] `/hq/projects/<int:project_id>/final-review` ルートが実装されている
- [x] 本部管理者ロールのみ表示・操作できるようにしている
- [x] `hq_pending` 以外の案件は審査対象外として扱う
- [x] 本部承認待ち案件がない場合の空状態テンプレートが用意されている
- [x] 画面上部に `_flash.html` が追加されている

---

【手動テスト】
- [x] 本部管理者で最終承認審査画面を開ける
- [x] 本部承認待ち案件が表示される
- [x] 案件切り替えボタンで別の審査待ち案件へ移動できる
- [ ] 本部承認待ち案件がない場合、空状態画面が表示される

---

### 申請内容・案件情報表示
【実装確認】
- [x] 案件名、申請者、部門名、目的・概要、予算、工数、実施期間、申請日、案件番号が実データ表示になっている
- [x] 部門名は申請者所属ではなく、案件に紐づく部門名を表示している
- [x] 部門ラベルは既存の部門ラベルclassを使う設計になっている
- [x] 待機日数バッジは部門承認日側に表示される構造になっている

---

【手動テスト】
- [x] 申請内容カードの表示が実データになっている
- [x] 部門名ラベルが表示されている
- [x] 申請日、部門承認日、案件番号が表示されている

---

### 全社予算インパクトシミュレーション
【実装確認】
- [x] 全社年間予算は対象年度の部門年間予算合計から算出している
- [x] 予算年度は開始予定日をもとに日本年度で判定している
- [x] 本案件金額、全社実績額、予算消化率、予算残高、全社占有率、全社内順位を表示している
- [x] ok / warn / danger の判定が実装されている
- [x] `10%` が `1%` にならない割合表示の方針になっている
- [x] 予算超過時の残予算は `-¥1,000,000` 形式で表示する方針になっている

---

【手動テスト】
- [x] 予算シミュレーションカードに実データが表示されている
- [x] 予算消化率、予算残高、全社占有率、全社内順位が表示されている
- [x] グラフと凡例が表示されている
- [x] アラートの配色と文言が表示内容に合っている

---

### 最終承認・却下処理
【実装確認】
- [x] 最終承認時に `status = in_progress` へ更新する処理がある
- [x] 最終承認時に `approval_stage = approved` へ更新する処理がある
- [x] 最終承認時に `approved_budget_amount` を保存する処理がある
- [x] 却下時に `status = rejected` へ更新する処理がある
- [x] 却下時に `approval_stage = rejected` へ更新する処理がある
- [x] 却下理由を保存する処理がある
- [x] `ProjectStatusLog` を作成する処理がある
- [x] 申請者向け通知を作成する処理がある
- [x] 重要処理は確認モーダル経由でPOSTする構造になっている

---

【手動テスト】
- [x] 予算確定チェックを入れると最終承認ボタンを押せる
- [x] 予算確定チェック未チェック時はtoastで案内される
- [x] 最終承認ボタンを押すと確認モーダルが表示される
- [x] 却下選択時、却下理由欄とテンプレートが表示される
- [x] 却下理由未入力時はtoastと赤枠が表示される
- [x] 却下理由入力後、却下確認モーダルが表示される

---

### flash / toast 表示整理
【実装確認】
- [x] サーバー処理結果はflashで表示する方針になっている
- [x] 画面内の軽い入力誘導は画面個別toastで表示する方針になっている
- [x] 成功結果をtoastで表示するモック処理はPOST submitに置き換えられている
- [x] `_toast.html` にflashを読ませる実装へ戻していない
- [x] base.htmlから共通toastのincludeを外している

---

【手動テスト】
- [x] 最終承認成功時はflashが表示される
- [x] 却下成功時はflashが表示される
- [x] 最終承認成功時に成功toastが二重表示されない
- [x] 却下成功時に成功toastが二重表示されない
- [x] テンプレート挿入時はtoastが表示される

---

### モバイル表示調整
【実装確認】
- [x] スマホ幅でカードが画面幅内に収まるようにCSS調整している
- [x] スマホ幅で基本情報カード、予算カード、判定カード、キューカードが横にはみ出しにくい指定になっている
- [x] スマホ幅でも予算シミュレーションの小カードは2列表示になっている
- [x] PC表示の3カラム指定は変更していない
- [x] タブレット表示の2カラム指定は変更していない

---

【手動テスト】
- [x] スマホ幅で「申請内容（基本情報）」カードが見切れない
- [x] スマホ幅で「全社予算インパクト シミュレーション」カードが見切れない
- [x] スマホ幅で予算カード内の小カードが2列で表示される
- [x] スマホ幅で「最終判定・予算確定」カードが見切れない
- [x] スマホ幅で「審査待ちキュー」カードが見切れない
- [x] PC表示で3カラムレイアウトが崩れていない

---

## ■ 申請者：案件進捗管理画面

### 進捗・予算・月次報告の更新
【実装確認】
- [x] 進捗率・ステータス・予算実績・月次報告を一括更新できるPOST処理がある
- [x] 更新前に確認モーダルを表示する処理がある
- [x] 未保存状態に応じて「更新する」「変更を戻す」ボタンを制御している

---

【手動テスト】
- [x] 進捗率・ステータスを変更すると未保存状態になる
- [x] 「更新する」→確認モーダル→更新で変更内容が保存される
- [x] 「変更を戻す」で画面読み込み時点の状態に戻る
- [x] 予算実績を反映して更新すると、累計実績が保存される
- [x] 月次報告を入力して更新すると、入力内容が保存される

---

### タスク追加・編集
【実装確認】
- [x] タスク追加用のPOSTルートがある
- [x] タスク編集用のPOSTルートがある
- [x] タスク追加時の初期ステータスは未着手、進捗率は0%になっている
- [x] タスク編集では、ステータス・進捗率を変更しない
- [x] 開発中案件であれば、完了済みタスクも基本情報を編集できる
- [x] completed案件ではタスク追加・編集できないよう制限している

---

【手動テスト】
- [x] タスク追加モーダルからタスクを追加できる
- [x] 追加したタスクが一覧に表示される
- [x] タスク編集モーダルにタスク名・担当者名・開始日・期限日が初期表示される
- [x] タスク編集モーダルから基本情報を保存できる
- [x] 未保存の進捗変更がある状態でタスク追加・編集ボタンを押すと、モーダルが開かずtoastが表示される

---

### タスク削除
【実装確認】
- [x] タスク削除用のPOSTルートがある
- [x] タスク削除は物理削除で実装している
- [x] 申請者本人の開発中・承認済み案件のみ削除できる
- [x] 最後の1件のタスクは削除できない
- [x] タスク削除では案件を自動でcompletedにしない
- [x] 削除成功時はflashを表示する方針になっている
- [x] 編集ボタンと削除ボタンは縦並びで表示される

---

【手動テスト】
- [x] ゴミ箱アイコンを押すと削除確認モーダルが開く
- [x] 削除確認モーダルのキャンセルで削除されずに閉じる
- [x] 「削除する」でタスクを削除できる
- [x] 削除後、同じ案件進捗管理画面へ戻る
- [x] 削除後、削除したタスクが一覧から消える
- [ ] 最後の1件のタスクは削除できない
- [x] 未保存の進捗変更がある状態で削除ボタンを押すと、削除確認モーダルが開かずtoastが表示される

---

### スマホ表示
【実装確認】
- [x] スマホ表示時のタスク追加・編集モーダルの高さを調整している
- [x] タスク追加・編集モーダル内をスクロールできる
- [x] 小さい画面でモーダル下部の操作ボタンを押しやすくしている

---

【手動テスト】
- [x] スマホ表示でタスク追加・編集モーダルの見出しやボタンが見切れない
- [x] スマホ表示でモーダル内を自然にスクロールできる
- [x] スマホ表示で編集・削除ボタンの縦並びが崩れていない

---

## ■ 部門管理者：案件モニタリング画面

### 案件モニタリング表示・完了認定
【実装確認】
- [x] `/manager/projects/monitoring` がログイン必須・部門管理者限定になっている
- [x] 自部門の開発中・承認済み案件のみ取得する条件になっている
- [x] 対象案件が0件の場合の空状態テンプレートがある
- [x] 案件切り替えタブが実データ表示になっている
- [x] `active_menu` が `manager_monitoring` に修正されている
- [x] 案件サマリー、進捗率、予算消費率、月次進捗報告、タスク一覧が `view_data` から表示される
- [x] タスク一覧に「全て / 遅延 / 進行中 / 未着手 / 完了」のフィルタがある
- [x] スマホ表示でフィルタボタンが折り返されないように調整されている
- [x] 完了認定ボタンが条件未達時に disabled になる
- [x] 完了認定は確認モーダル経由でPOST送信する
- [x] 完了認定POST時にサーバー側で完了条件を再チェックしている
- [x] 完了認定時に `Project.status`、`completed_at`、`updated_at` を更新している
- [x] 完了認定時に `ProjectStatusLog(action="complete")` を作成している
- [x] 完了認定時に主担当者向けの `Notification(type="completed")` を作成している
- [x] 申請者の進捗更新POST成功時に `project.updated_at = utc_now()` を明示更新している
- [x] `models.py` とマイグレーションは変更していない

---

【手動テスト】
- [x] 部門管理者で `/manager/projects/monitoring` にアクセスすると、案件モニタリング画面が表示される
- [x] 案件切り替えタブを押すと、別案件のモニタリング画面へ移動できる
- [x] タスク一覧の「全て / 遅延 / 進行中 / 未着手 / 完了」フィルタが動く
- [x] スマホ表示でタスクフィルタボタンが横スクロールでき、文字が2行に崩れない
- [x] 進捗率が100％と99％以下で棒グラフとパーセント表示の色が変わる
- [x] 遅延タスクのある案件がちゃんと案件切り替えボタン・案件サマリー・タスク一覧テーブルに反映される
- [x] 予算注意のある案件がちゃんと案件切り替えボタン・案件サマリーに反映される
- [x] 月次進捗報告の提出、未提出がちゃんと案件サマリーに反映される
- [x] 予算消費状況が79％以下、80％以上、100％以上で棒グラフとパーセント表示の色が変わる
- [x] 完了条件を満たしていない案件では、完了認定ボタンがグレーアウトしている
- [x] 完了条件を満たしている案件では、完了認定ボタンから確認モーダルが開く
- [x] 確認モーダルで「見直す」を押すと、モーダルが閉じる
- [x] 完了認定を実行すると、成功flashが表示される
- [x] 完了認定した案件への案件切り替えボタンは表示されなくなる
- [ ] モニタリング対象案件がない場合、空状態と「部門管理者トップへ戻る」リンクが表示される 

---

## ■ 部門管理者トップ画面

### ダッシュボード実データ化・通知表示整理
【実装確認】
- [x] `/top/manager` で `manager_top.html` を表示するルートが実装されている
- [x] 部門管理者以外がアクセスした場合、ロール別トップへ戻す権限制御がある
- [x] 部門管理者トップ画面用の表示データを `build_manager_top_view_data()` にまとめている
- [x] サマリーバンドの件数が実データから作成されている
- [x] サマリーバンドの数字色が、承認待機・負荷集中・遅延・予算注意/超過の条件で切り替わる
- [x] 承認依頼リストが、自部門の部門承認待ち案件から作成されている
- [x] 完了認定依頼リストが、全タスク完了状態の開発中案件から作成されている
- [x] 完了認定依頼リストは、待機が長いものから確認できるよう依頼日が古い順に並ぶ
- [x] タスク期限日が未設定でも、遅延判定で500エラーにならないよう `due_date` のNoneガードがある
- [x] 自部門案件一覧が、開発中・完了済み案件から作成されている
- [x] 自部門案件一覧の状態ラベルに、完了認定待ち・遅延・予算注意・予算超過・進行中・完了の判定がある
- [x] 完了済み案件のみ、行背景と案件名の太字ルールが切り替わる
- [x] 自部門案件一覧のフィルタは、ページ遷移なしで表示行を切り替える
- [x] フィルタ結果0件時は、空状態文言を表示しない
- [x] 自部門案件一覧の本当のデータ0件時のみ、空状態文言を表示する
- [x] テーブル系空状態から「データがまだありません」の主文言を削除している
- [x] メンバー負荷・リソース状況が、未完了タスク数・遅延タスク数をもとに作成されている
- [x] メンバー負荷の凡例色が、実際のバー色と一致している
- [x] 部門年間予算 執行率カードが、年度予算・執行済・残余・超過分を実データから作成している
- [x] 部門年間予算ドーナツは、表示用文字列ではなく数値用データを使って描画している
- [x] 超過分0円のとき、ドーナツ本体に赤い点が出ないよう制御している
- [x] 年間予算未登録時は、ドーナツではなくカード内空状態を表示する
- [x] 共通toastは、ログイン成功などの軽い通知だけを表示する
- [x] flashはtoast化せず、`base.html` の共通 `_flash.html` で表示する
- [x] `base.html` 継承画面のflash表示は、共通レイアウト側に統一されている
- [x] `manager_top.html` の画面ローカルtoastは、完了済み案件の詳細未実装通知用として残している
- [x] `get_fiscal_year()` は、部門管理者トップ画面の表示データ作成関数より上に配置されている
- [x] `python -m py_compile app.py` が成功している

---

【手動テスト】
- [x] 部門管理者アカウントでログインすると、ログイン成功toastが画面下部中央に表示される
- [x] `/top/manager` が表示できる
- [x] サマリーバンドの件数・補足文言が画面上で不自然に崩れていない
- [x] サマリーバンドの数字色が、0件の項目では白系で表示される
- [x] 承認依頼がある場合、承認依頼リストに案件が表示される
- [x] 承認依頼リストの「審査へ →」から承認審査画面へ遷移できる
- [x] 完了認定依頼がある場合、完了認定依頼リストに案件が表示される
- [x] 完了認定依頼リストの「完了認定へ →」から案件モニタリング詳細画面へ遷移できる
- [x] 自部門案件一覧の「全て」フィルタで案件が表示される
- [x] 自部門案件一覧の「進行中」「遅延」「完了」フィルタを押すと、表示行が切り替わる
- [x] フィルタ対象がない場合、不要な「選択中の条件に該当する案件はありません。」が表示されない
- [x] 自部門案件一覧のPC表示で、列配置が不自然に右寄り・左寄りになっていない
- [x] 自部門案件一覧のスマホ表示で、カード風レイアウトが崩れていない
- [x] 開発中案件の「詳細 →」から案件モニタリング詳細画面へ遷移できる
- [x] 完了済み案件の「詳細 →」を押すと、画面下部toastで未実装メッセージが表示される
- [x] メンバー負荷・リソース状況のバーと凡例色が一致して見える
- [x] 部門年間予算 執行率カードで、超過分0円のときにドーナツ本体へ赤い点が出ていない
- [x] flashが発生する操作後、メインコンテンツ上部にflashが表示される
- [x] エラーや注意のflashが、下部toastではなく画面上部flashとして表示される

---

## ■ 本部管理者トップ画面

### ダッシュボード実データ表示
【実装確認】
- [x] `/top/hq` ルートが本部管理者向けトップ画面として実装されている
- [x] 本部管理者以外は `/top/hq` を表示できないように権限制御されている
- [x] `hq_top.html` に `view_data` を渡して実データ表示する構成になっている
- [x] 通常のデータ0件時は `hq_top.html` 内の空状態で表示する方針になっている
- [x] DB例外時のみ `hq_top_empty.html` を表示する方針になっている
- [x] `hq_top_empty.html` は `base.html` を継承している
- [x] 未読通知件数 `unread_notifications_count` をテンプレートへ渡している

---

【手動テスト】
- [x] 本部管理者アカウントでログインすると `/top/hq` を表示できる
- [x] `/top/hq` を開いて500エラーが出ない
- [x] 画面ヘッダーに「ダッシュボード」と説明文が表示される
- [x] 本部管理者トップ画面の全体レイアウトが大きく崩れていない

---

### サマリーバンド
【実装確認】
- [x] 部門案件は `rejected` を除外して集計されている
- [x] 最終承認依頼は `status == "hq_pending"` かつ `approval_stage == "hq_pending"` を対象にしている
- [x] 最終承認依頼の待機日起算日は `approve_department → updated_at → created_at` に統一されている
- [x] 予算注意部門は部門年間予算の執行率をもとに判定されている
- [x] 全社予算執行率は全部門の年間予算合計と当年度の予算実績合計から計算されている
- [x] 遅延案件は進行中案件の期限切れ未完了タスクをもとに判定されている
- [x] 予算注意案件は進行中案件のみを対象にしている
- [x] サマリーバンドの数字色は class で制御されている

---

【手動テスト】
- [x] サマリーバンドに「部門案件」「最終承認依頼」「予算注意部門」「全社予算執行率」「遅延案件」「予算注意案件」が表示される
- [x] 数値と単位が見やすく表示されている
- [x] 注意・警告状態の数値色が画面上で不自然になっていない

---

### 部門別予算執行率カード
【実装確認】
- [x] 全ての部門を表示対象にしている
- [x] 年間予算未登録部門も表示対象にしている
- [x] 部門別の予算注意件数を事前集計する形に整理されている
- [x] 予算注意/超過案件タグは進行中案件のみを対象にしている
- [x] 部門別予算執行率の横棒は `data-target-width` を使ってアニメーション表示できる
- [x] 年間予算未登録部門には未登録用の横棒classを使っている

---

【手動テスト】
- [x] 「部門別予算執行率」カードが表示される
- [x] 部門ごとの横棒グラフが表示される
- [x] 横棒グラフが0%から実値幅へ伸びる
- [x] 案件数・遅延・予算超過・予算注意・予算健全・年間予算未登録のタグ表示が不自然でない

---

### 全社年間予算 執行率カード
【実装確認】
- [x] 全社年間予算と当年度予算実績から執行率を計算している
- [x] 執行率は整数％表示になっている
- [x] 全社年間予算未登録時はカード内空状態を表示する
- [x] 空状態文言は「全社年間予算が未登録です。」になっている
- [x] ドーナツグラフは `data-target-dasharray` を使ってアニメーション表示できる
- [x] `over_dashoffset` が `-0.00` にならないように調整されている

---

【手動テスト】
- [x] 「全社年間予算 執行率」カードが表示される
- [x] 年間予算がある場合、ドーナツグラフが0から実値へ描画される
- [x] 執行率・執行済・超過分・残余・年間予算が見やすく表示される
- [x] 年間予算未登録時の空状態文言が不自然でない

---

### 全案件 フェーズ分布カード
【実装確認】
- [x] 完了・開発進行中・承認待ち・遅延中・却下の5分類で表示できる
- [x] 遅延中案件は開発進行中に二重カウントされない
- [x] 0件フェーズも凡例に表示できる
- [x] 全案件0件時はカード内空状態を表示する
- [x] フェーズ分布ドーナツは `data-target-dasharray` を使ってアニメーション表示できる

---

【手動テスト】
- [x] 「全案件 フェーズ分布」カードが表示される
- [x] ドーナツグラフが0から各セグメントへ描画される
- [x] 凡例の件数と割合が見やすく表示される
- [x] 完了・開発進行中・承認待ち・遅延中・却下の表示が不自然でない

---

### 最終承認依頼テーブル
【実装確認】
- [x] `hq_pending` かつ `approval_stage == "hq_pending"` の案件を表示対象にしている
- [x] 部門承認日は `approve_department → updated_at → created_at` の優先順位で取得している
- [x] ソート順は部門承認日 ASC / project_id ASC に揃えている
- [x] 案件に紐づく部門を表示している
- [x] 「確定予算」ではなく「概算予算」表示になっている
- [x] 案件名と「審査へ →」が最終承認審査画面へのリンクになっている
- [x] テーブル内で承認・却下などのPOST処理を行わない
- [x] 最終承認審査画面側の本部承認待ち案件順も同じ起算日ルールに統一されている

---

【手動テスト】
- [x] 最終承認依頼がある場合、テーブルに案件が表示される
- [x] 「審査へ →」をクリックすると最終承認審査画面へ移動できる
- [x] 案件名クリックでも最終承認審査画面へ移動できる
- [x] 最終承認依頼が0件の場合、空状態文言が表示される

---

### 全案件一覧テーブル
【実装確認】
- [x] `department_pending` / `hq_pending` / `in_progress` / `completed` / `rejected` を表示対象にしている
- [x] 不明ステータスの案件を進行中扱いにしない
- [x] 初期表示順は要注意案件が上に来るように制御されている
- [x] 進捗率は完了タスク数 ÷ 全タスク数で計算されている
- [x] 遅延案件でも進捗ゲージ自体は赤にしない
- [x] 却下案件の進捗・予算消費・月次報告は控えめ表示になっている
- [x] 月次報告の色は更新日の鮮度で判定されている
- [x] 展開行は申請概要・最新月次報告・最終報告で出し分けられる
- [x] 検索・部門フィルタ・状態チップはフロントエンドで絞り込む
- [x] フィルタで非表示になった展開行は閉じた状態へ戻る
- [x] ソート矢印とモック用toastは削除されている

---

【手動テスト】
- [x] 全案件一覧テーブルが表示される
- [x] 検索欄に入力すると案件一覧が絞り込まれる
- [x] 部門プルダウンで案件一覧が絞り込まれる
- [x] 状態チップで「全て」「遅延」「進行中」「承認待ち」「完了」の絞り込みができる
- [x] `+` ボタンで詳細行が開く
- [x] もう一度押すと詳細行が閉じる
- [x] 絞り込み結果が0件のとき「条件に一致する案件がありません。」が表示される
- [x] 完了・却下行が控えめな見た目で表示される

---

### レスポンシブ・JS挙動
【実装確認】
- [x] スマホ幅では最終承認依頼カードがアコーディオン表示になる
- [x] PC幅では最終承認依頼カードが常時表示される
- [x] アコーディオンの初期化二重呼び出しは整理されている
- [x] 展開ボタンには `type="button"` と `aria-expanded` が設定されている
- [x] グラフアニメーションはJSで再計算せず、Python側で作成した値をdata属性から反映する

---

【手動テスト】
- [x] PC表示で最終承認依頼カードが開いた状態で表示される
- [x] スマホ幅で最終承認依頼カードを開閉できる
- [x] 検索・フィルタ・展開ボタン操作時に画面上のエラーが出ない
- [x] グラフアニメーション後も検索・フィルタ・展開ボタンが動く

---

## ■ 申請者トップ画面

### 申請者トップ画面の実データ表示・導線整備
【実装確認】
- [x] `/top/applicant` が申請者トップ画面として実装されている
- [x] `@login_required` が設定されている
- [x] `require_applicant()` により申請者ロール専用画面になっている
- [x] `demo_role="applicant"` を渡さず、通常のロール判定に寄せている
- [x] `view_data` を使ってテンプレートへ表示用データを渡している
- [x] `unread_notifications_count` をテンプレートへ渡している
- [x] 申請中案件はログイン中申請者本人の案件のみ取得している
- [x] 申請中案件の対象は `department_pending` / `hq_pending` / `rejected` に限定されている
- [x] 申請中案件カードに案件名・部門名・申請日・承認ステップが表示できる構造になっている
- [x] 申請中案件カードの部門名は `Project.department` から取得している
- [x] 申請日表示は `submit` ログ優先、なければ `Project.created_at` を使う実装になっている
- [x] 申請中案件カードの「詳細」は申請状況確認・管理画面へ遷移するリンクになっている
- [x] 却下案件の「修正・再申請」は未実装トースト表示になっている
- [x] 開発管理中案件は `in_progress` かつ `approval_stage == "approved"` の本人案件のみ取得している
- [x] 開発管理中案件カードでは、案件名・部門名・完了予定日・全体進捗・予算消費率が表示できる構造になっている
- [x] 開発管理中案件の遅延判定は未完了タスクの `due_date` を基準にしている
- [x] `planned_end_date` は「完了予定」の表示用であり、遅延判定には使っていない
- [x] 予算ラベルの文言は `予算` に統一されている
- [x] 予算80%以上はオレンジ系、100%以上は赤系のclassで出し分ける実装になっている
- [x] 予算ゲージ幅は100%を上限にしている
- [x] 注意タスクは本人の開発管理中案件に紐づく未完了タスクのみ対象になっている
- [x] 完了済み案件のタスクは注意タスクに表示されない
- [x] 遅延タスクは `due_date < jst_today()` で判定している
- [x] 3日以内が期限のタスクは今日・明日・2日後を対象にしている
- [x] 注意タスク0件時は見出し横に `なし` を表示し、本文行を出さない構造になっている
- [x] スマホ表示では注意タスクカードがアコーディオンになる
- [x] スマホ表示の承認ステータスカード上部は、案件名・申請日・部門名の順で縦並びになる
- [x] 固定フッターの「＋ 新規案件申請」は新規案件申請画面へのリンクになっている
- [x] DB取得エラー時は `rollback`、flash表示、空の `view_data` で通常テンプレートを表示する実装になっている
- [x] 申請状況確認・管理画面へのサイドバー用入口ルート `/applicant/projects/status` が追加されている
- [x] 申請者サイドバーから申請者トップ・新規案件申請・申請状況確認・案件進捗管理へ移動できるリンクが追加されている
- [x] 申請者サイドバーの「修正して再申請」はリンク化せず、未実装表示になっている
- [x] 部門管理者・本部管理者のサイドバーにも実装済み主要画面へのリンクが追加されている
- [x] `alert(...)` の仮実装は残っていない
- [x] 申請者トップ画面に不要なPOST処理は追加されていない
- [x] `models.py` やマイグレーションは変更していない

---

【手動テスト】
- [x] 申請者アカウントでログインし、申請者トップ画面が表示される
- [x] 申請中案件の承認ステータスカードに、案件名・申請日・部門名が表示される
- [x] 申請中案件の承認ステップが、部門承認待ち・本部承認待ち・却下の状態に応じて表示される
- [x] 申請中案件カードの「詳細」から申請状況確認・管理画面へ移動できる
- [x] 却下案件の「修正・再申請」を押すと、未実装トーストが表示される
- [x] 開発管理中案件カードに、案件名・部門・完了予定・全体進捗・予算消費率が表示される
- [x] 開発管理中案件カードの「クイック更新」から案件進捗管理画面へ移動できる
- [x] 遅延タスクがある場合、注意タスクカードに表示される
- [x] 3日以内が期限のタスクがある場合、注意タスクカードに表示される
- [x] 注意タスクの行をクリックすると、該当案件の案件進捗管理画面へ移動できる
- [x] 注意タスクが0件の場合、見出し横に `なし` と表示される
- [x] 固定フッターの「＋ 新規案件申請」から新規案件申請画面へ移動できる
- [x] 申請者サイドバーから、ダッシュボードへ移動できる
- [x] 申請者サイドバーから、新規案件の申請へ移動できる
- [x] 申請者サイドバーから、申請状況の確認・管理へ移動できる
- [x] 申請者サイドバーから、案件の進捗管理へ移動できる
- [x] 申請者サイドバーの「修正して再申請」はクリックしても遷移しない
- [x] PC表示で「修正して再申請」にマウスを乗せると `未実装です` と表示される
- [x] スマホ表示で「修正して再申請（未実装）」と表示される
- [x] スマホ表示で承認ステータスカード上部が、案件名・申請日・部門名の順で縦並びになっている
- [x] スマホ表示で注意タスクカードをタップすると開閉できる
- [x] 部門管理者アカウントで `/top/applicant` を直接開こうとしても、申請者トップ画面は表示されない
- [x] 本部管理者アカウントで `/top/applicant` を直接開こうとしても、申請者トップ画面は表示されない

---